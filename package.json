{
  "name": "repo-chat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
