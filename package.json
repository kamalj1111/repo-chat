{
  "name": "repo-chat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
