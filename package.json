{
  "name": "repo-chat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
