{
  "name": "repo-chat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
